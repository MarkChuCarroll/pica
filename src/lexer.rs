#[derive(Clone, Debug)]
pub struct LexicalError {
    pub msg: String,
    pub pos: usize,
}

#[derive(Clone, Debug)]
pub enum Token {
    Comma,
    Bar,
    Colon,
    ColonColon,
    ColonEq,
    Div,
    Dot,
    Equal,
    EqualEqual,
    GE,
    GT,
    LE,
    LT,
    Minus,
    Mod,
    NotEqual,
    Plus,
    Times,

    LParen,
    RParen,
    LBrack,
    RBrack,
    LCurly,
    RCurly,
    LCH, // <<
    RCH, // >>

    AtBehavior,
    AtBoson,
    AtCond,
    AtFlavor,
    AtFor,
    AtQuark,
    AtWhile,

    Adopt,
    And,
    Behavior,
    Boson,
    Both,
    Chan,
    Cond,
    Create,
    Do,
    End,
    Else,
    Exit,
    Flavor,
    For,
    Hadron,
    In,
    Is,
    Not,
    On,
    Or,
    Out,
    Par,
    Provides,
    Quark,
    Rec,
    Select,
    Send,
    Seq,
    Slot,
    Then,
    Use,
    Var,
    While,

    Symbol(String),
    TypeVar(String),
    StrLit(String),
    IntLit(i64),
    FloatLit(f64),
    CharLit(char),
}
