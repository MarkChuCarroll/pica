package org.goodmath.pica.qgp;

/**
 * setField(reg1, bosonType, bosonField, reg2) - update the specified field of the boson stored
 * in reg1 with the value stored in reg2.
 */
public class BSetField extends Instruction {
    public BSetField(Reg boson, String bosonType, String bosonField, Reg val) {

    }
}
