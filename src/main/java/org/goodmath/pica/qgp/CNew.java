package org.goodmath.pica.qgp;

/**
 * CNew(addr) -- create a new continuation from the current quark, a copy of the
 * current ray's local registers, and the address of an instruction.
 */
public class CNew extends Instruction {
    public CNew(int address) {

    }
}
